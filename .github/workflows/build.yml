on:
  push:
    branches: main

job:
  build:
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '^1.17.3'
      - run: |
          go version
          cd firstProgram/wc
          go test -v
          go build
